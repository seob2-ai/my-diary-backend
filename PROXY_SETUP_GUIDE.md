# 프록시 설정 및 문제 해결 가이드

## 문제 상황

클라이언트가 `http://localhost:5173/api/diary`로 요청을 보내고 있지만, 백엔드는 `http://localhost:8000`에서 실행되고 있습니다.

## 해결 방법

### 1. 프론트엔드 Vite 프록시 설정 확인

프론트엔드 프로젝트의 `vite.config.js` 또는 `vite.config.ts` 파일에 다음 설정이 있어야 합니다:

```javascript
export default {
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      }
    }
  }
}
```

### 2. 백엔드 서버 실행 확인

백엔드 서버가 실행 중인지 확인:

```bash
# 방법 1: 배치 파일 실행
start_server.bat

# 방법 2: 직접 실행
python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### 3. 서버 로그 확인

서버를 실행한 터미널 창에서 다음 로그를 확인하세요:

- `요청 시작: POST http://localhost:8000/api/diary` - 요청이 도착했는지 확인
- `일기 생성 요청 시작: user_id=...` - 요청이 처리되기 시작했는지 확인
- 에러 메시지 - 어떤 에러가 발생했는지 확인

### 4. 로그 파일 확인

서버를 실행하면 `logs/server.log` 파일에 모든 로그가 저장됩니다.
이 파일을 열어서 에러 메시지를 확인하세요.

## 현재 상태 확인

1. **백엔드 서버 실행 중**: ✅ (포트 8000에서 실행 중)
2. **프록시 설정**: ❓ (프론트엔드 설정 확인 필요)
3. **에러 로그**: ❓ (서버 로그 확인 필요)

## 다음 단계

1. `start_server.bat` 파일을 실행하여 서버를 시작하세요
2. 서버 창에서 로그를 확인하세요
3. 클라이언트에서 일기 저장을 시도하세요
4. 서버 창에 출력된 로그를 확인하세요

## 로그 확인 포인트

클라이언트에서 일기 저장을 시도하면 다음 중 하나가 출력됩니다:

### 정상적인 경우:
```
요청 시작: POST http://localhost:8000/api/diary
일기 생성 요청 시작: user_id=...
일기 생성 성공: id=...
응답 완료: 201
```

### 에러가 발생한 경우:
```
요청 시작: POST http://localhost:8000/api/diary
미들웨어에서 예외 발생: ...
또는
전역 예외 핸들러에서 예외 발생: ...
또는
ServerErrorMiddleware에서 서버 에러 발생: ...
```

**에러 메시지 전체를 복사해서 공유해주시면 정확한 원인을 파악할 수 있습니다!**

