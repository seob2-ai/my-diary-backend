# 리팩토링 계획서

## 프로젝트 분석 결과

### 발견된 주요 이슈

#### 🔴 **긴급 (Critical) - 즉시 수정 필요**

1. **누락된 스키마 정의**
   - `WeeklySummaryResponse`가 `schemas.py`에 정의되지 않았지만 `summary.py`에서 사용됨
   - 위치: `app/routers/summary.py:24, 79`
   - 영향: 런타임 에러 발생 가능

2. **보안 취약점**
   - CORS 설정이 `allow_origins=["*"]`로 모든 출처 허용
   - 위치: `app/main.py:31`
   - 영향: 프로덕션 환경에서 보안 위험

3. **에러 처리 부족**
   - `parse_date_or_today` 함수에 날짜 파싱 에러 처리 없음
   - 위치: `app/routers/diary.py:14-17`
   - 영향: 잘못된 날짜 형식 입력 시 500 에러 발생

4. **데이터베이스 세션 관리**
   - 트랜잭션 롤백 처리가 없음
   - 위치: `app/routers/diary.py:103-105`
   - 영향: 에러 발생 시 데이터 일관성 문제

#### 🟡 **높음 (High) - 빠른 시일 내 수정 권장**

5. **중복 코드 제거**
   - `generate_uuid()`와 `make_diary_id()` 중복
   - 위치: `app/models.py:8-14`
   - 영향: 유지보수성 저하

6. **사용되지 않는 코드**
   - `generate_coaching()` 함수가 정의되어 있지만 사용되지 않음
   - 위치: `app/services/analysis_service.py:182-225`
   - 영향: 코드 복잡도 증가, 혼란 야기

7. **일관성 없는 사용자 인증 처리**
   - `diary.py`는 직접 `Header` 사용, `summary.py`는 `get_user_id` 의존성 사용
   - 위치: `app/routers/diary.py:31`, `app/routers/summary.py:13-19`
   - 영향: 코드 일관성 저하

8. **하드코딩된 값들**
   - 데이터베이스 URL, 모드 우선순위 등이 하드코딩됨
   - 위치: `app/database.py:5`, `app/routers/summary.py:72`
   - 영향: 설정 변경 시 코드 수정 필요

9. **타입 힌트 부족**
   - 일부 함수의 반환 타입이 명시되지 않음
   - 위치: `app/routers/diary.py:14`, `app/models.py:8, 12`
   - 영향: 코드 가독성 및 IDE 지원 저하

10. **분석 결과 정규화 로직의 복잡성**
    - `analyze_mode` 반환값을 정규화하는 복잡한 로직
    - 위치: `app/routers/diary.py:76-85`
    - 영향: 유지보수 어려움, 버그 발생 가능성

#### 🟢 **중간 (Medium) - 점진적 개선**

11. **환경 변수 관리 부재**
    - `.env` 파일이나 환경 변수 관리 시스템 없음
    - 영향: 설정 관리 어려움, 배포 환경별 설정 불가

12. **로깅 시스템 부재**
    - 로깅이 전혀 구현되지 않음
    - 영향: 디버깅 및 모니터링 어려움

13. **업데이트 시간 필드 문제**
    - `updated_at`이 `onupdate`만 있고 트리거가 제대로 작동하지 않을 수 있음
    - 위치: `app/models.py:37`
    - 영향: 업데이트 시간이 정확히 기록되지 않을 수 있음

14. **중복된 필드 처리 로직**
    - `payload.emotion or ""` 패턴이 반복됨
    - 위치: `app/routers/diary.py:67-72, 91-95`
    - 영향: 코드 중복, 유지보수 어려움

15. **비동기 처리 미적용**
    - 모든 라우터가 동기 함수로 구현됨
    - 영향: 성능 저하 가능성

16. **테스트 코드 부재**
    - 단위 테스트, 통합 테스트가 없음
    - 영향: 코드 변경 시 회귀 테스트 불가

17. **API 문서화 개선**
    - 일부 엔드포인트에 설명이 부족함
    - 영향: API 사용자 경험 저하

#### 🔵 **낮음 (Low) - 장기적 개선**

18. **의존성 주입 개선**
    - 데이터베이스 세션 외 다른 의존성 관리 체계 부재
    - 영향: 확장성 제한

19. **리팩토링: 분석 서비스 구조**
    - `analysis_service.py`가 단일 파일에 모든 로직 포함
    - 영향: 파일이 길어질수록 유지보수 어려움

20. **코드 주석 및 문서화**
    - 일부 함수에 docstring이 없거나 부족함
    - 영향: 코드 이해도 저하

---

## 우선순위별 리팩토링 계획

### Phase 1: 긴급 수정 (즉시)

1. ✅ `WeeklySummaryResponse` 스키마 추가
2. ✅ CORS 설정 환경 변수화 및 제한
3. ✅ `parse_date_or_today` 에러 처리 추가
4. ✅ 트랜잭션 롤백 처리 추가

### Phase 2: 높은 우선순위 (1주일 내)

5. ✅ 중복 함수 제거 (`make_diary_id` 제거)
6. ✅ `generate_coaching` 함수 통합 또는 제거
7. ✅ 공통 사용자 인증 의존성 함수 생성
8. ✅ 하드코딩된 값들을 설정 파일로 이동
9. ✅ 타입 힌트 보완
10. ✅ 분석 결과 정규화 로직 개선

### Phase 3: 중간 우선순위 (1개월 내)

11. ✅ 환경 변수 관리 시스템 구축 (python-dotenv)
12. ✅ 로깅 시스템 구축
13. ✅ `updated_at` 필드 수정
14. ✅ 필드 처리 헬퍼 함수 생성
15. ⚠️ 비동기 처리 전환 (선택적, 성능 이슈 시)
16. ✅ 테스트 코드 작성
17. ✅ API 문서화 개선

### Phase 4: 낮은 우선순위 (장기)

18. 의존성 주입 패턴 개선
19. 분석 서비스 모듈화
20. 코드 문서화 보완

---

## 예상 효과

### 가독성 향상
- 중복 코드 제거로 코드 길이 감소
- 타입 힌트 보완으로 IDE 지원 향상
- 일관된 패턴 적용

### 유지보수성 향상
- 환경 변수 관리로 설정 변경 용이
- 로깅 시스템으로 디버깅 시간 단축
- 테스트 코드로 회귀 버그 방지

### 효율성 향상
- 에러 처리 강화로 안정성 향상
- 트랜잭션 관리로 데이터 일관성 보장
- 비동기 처리로 성능 개선 (선택적)

---

## 참고 사항

- 모든 변경사항은 기존 API 인터페이스를 유지해야 함
- 단계별로 진행하며 각 단계마다 테스트 필요
- 프로덕션 배포 전 충분한 테스트 필수



